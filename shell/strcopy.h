#ifndef StrCopyIncluded		/* prevent multiple inclusion */
#define StrCopyIncluded

char *strcopy(char *inStr,int current);	/* like strdup */
char* strcat(char *str, char *secondStr);
int getLenght(char *str);
#endif // included
